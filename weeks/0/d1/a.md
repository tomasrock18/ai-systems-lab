# A1

event loop ожидает завершения handler, т.е. ждёт 10 секунд.
Если под под блокирующим http-запросом понимается что-то вроде requests.get(), то ничего бы не поменялось - handler по
прежнему ждал бы завершения внутреннего запроса.

# A2

Для себя, потому что не знал, пришлось искать - race condition это состояние, при которой несколько процессов/потоков
одновременно пытаются выполнить операции чтения или записи к общим ресурсам, без синхронизации.

Не знаю, что такое asyncio.Task, предположу что это отдельный поток.
Если исходить из моего предположения, тогда ответ - нет невозможно, поскольку в процессе всегда одновременно выполняется
только один поток.

# A3

Не знаю.

# A4

processes для вычисления SHA/математики и т.д., потому что сложные вычисления, поэтому параллелить стоить процессами.
threads для 10 000 открытых TCP-соединений, потому что нужна параллельная обработка input/output легких взаимодействий
asyncio для 100 зпросов к старому SDK, потому что бессмысленно на каждый запрос создавать отдельный поток, легче сделать
100 асинхронных запросов

# A5

Я не особо понимаю GIL, знаю что он вроде как гарантирует что в одном процессе интерпретатора работает всегда только 1
поток, следовательно любое python приложение можно считать однопоточным.
Поэтому GIL защищает от race condition потоков, однако race condition для процессов всё ещё возможно
Утверждение верно.

# A6
Нет, потому что процессы по-умолчанию, если не выстроен IPC не имеют общих ресурсов

# A7
Процесс event loop 

# A8
Отсутствие mutex, поведение при сбое того или иного потока/процесса, отсутствие контроля состояний потоков/процессов.