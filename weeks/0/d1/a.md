# A1

event loop ожидает завершения handler, т.е. ждёт 10 секунд.
Если под под блокирующим http-запросом понимается что-то вроде requests.get(), то ничего бы не поменялось - handler по
прежнему ждал бы завершения внутреннего запроса.

# A2

Немного поглядев справки, меняю предположение: asyncio.Task, это объект, контролирующий выполнение функции, т.е. не
обязательно отдельный поток.
Глядя на код из hint 1, следует что если в await произойдёт доступ к shared, то это в теории может привести к race
condition, ведь по сути может открыться "окно" для внешнего несанционированного досутпа.

# A3

Не знаю.

# A4

Согласен, имеет смысл поменять местами asyncio и threads, поскольку tcp соединения могут быть открыты но это не значит
что они что-то блокируют

# A5

Быстро взглянув на справку: GIL не позволяет одновременно выполняться нескольким потокам, т.е. в конкретный момент
времени всегда выполняется только один поток
Да, из этого следует атомарность операций над общим пространством памяти, другое дело, что я не совсем понимаю как
именно интерпретатор решает какой поток нужно выполнять в конкретный момент, видимо это и есть причина race condition

# A6

Нет, потому что процессы по-умолчанию, если не выстроен IPC не имеют общих ресурсов

# A7

Слишком сложно, нужно включить в обучение

# A8

Отсутствие mutex, поведение при сбое того или иного потока/процесса, отсутствие контроля состояний потоков/процессов.