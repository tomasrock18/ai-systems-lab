Пока я плохо знаю чем и что можно делать, поэтому обратился к поиску, без помощи ИИ

То что socket listen я нашёл через:

```shell
ss -a | grep 9000
```

Установленное соединение отловил через:

```shell
netstat -tn | grep 9000
```

PID сервер отловил через:

```shell
lsof -i:9000 -t
```

Отвечая на вопрос про accept() - не знаю, вероятно да, поскольку команда ```lsof -i:9000 -t``` выдаёт
```127.0.0.1:60280```, следовательно 60280 и есть создаваемый порт.
