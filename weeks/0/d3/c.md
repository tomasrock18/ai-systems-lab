# C1

Нет, такой вывод сделать нельзя, поскольку сервис B мог принять запрос и начать создание worker, но в процессе
выполнения сервис http мог заблокироваться, в результате чего было превышено ожидание запроса.

# C2

Та же самая, или любая другая, вплоть до отказа соединения
Идемподентность, я слышал это понятие, но строго определения не помню, но суть вроде в том, что один и тот же запрос
постоянно получает один и тот же ответ, т.е. два одинаковых запрос не меняют состояние системы, например PUT традиционно
делают таким, а POST нет.

# C3

АСК не будет отправлен, собственно всё

Ту систему у которой ведётся логирование таких вещей

# C4

Кроме буквального перевода технической разницы не знаю, даже не знаю к чему это применяется.

# C5

Отсутствие контроля над worker? Если worker делает что-то тяжёлое, например расчёты на CPU, мы просто зря тратим
мощности сервера.

# C6

Race condition. Из способов предотварщения знаю только продумывание параллельных вычислений на этапе проектирования.
