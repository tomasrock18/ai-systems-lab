# A1

package main это разметка .go файла, которая указывает что этот модуль точка в хода в программу
import "fmt" это импорт стандартной библиотеки fmt, которая позволяет выполнять операции ввода/вывода и форматирования
func это лексима для инициализации функции

Так и не понял что такое chan, никогда с ним не встречался

Вероятно, это операторы присвоения

Не знаю, предположу, что ограничивают максимальное число jobs

Не знаю, вероятно что-то вроде копирования массива

# A2 - A3

Не могу реализовать за отведённое время, go слишком базовый и слишком мало практики.

# A4

Не знаю